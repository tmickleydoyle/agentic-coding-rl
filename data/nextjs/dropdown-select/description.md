# Dropdown select

Implement a client component `Picker` in `components/Picker.tsx`:

- Accepts a prop `options: string[]`.
- Renders a `<select data-testid="select">` with one `<option>` per item.
- Renders `<span data-testid="picked">` showing the currently selected value.
- The first option is selected by default.

Default export.
