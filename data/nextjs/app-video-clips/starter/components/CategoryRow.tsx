'use client'

export default function CategoryRow(_props: {
  category: string
  count: number
  onFilter: (category: string) => void
}) {
  // TODO: render the cat-<category> row with its count and a filter button.
  return null
}
