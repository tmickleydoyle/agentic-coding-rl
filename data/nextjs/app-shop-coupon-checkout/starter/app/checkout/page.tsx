'use client'

export default function CheckoutPage() {
  // TODO: read-only summary (subtotal/discount/total, code when applied) + place-order, or a
  // checkout-empty message.
  return <section data-testid="page-checkout" />
}
