'use client'

export default function CartPage() {
  // TODO: render the add-products list (product-<id> + add-<id>) and the cart (lines with
  // qty inputs + remove and a cart-subtotal), or a cart-empty message.
  return <section data-testid="page-cart" />
}
