'use client'

export default function CouponsPage() {
  // TODO: list coupons, an code-input + apply-code + clear-coupon, and the applied state
  // (applied-ok / applied-warn / applied-error).
  return <section data-testid="page-coupons" />
}
