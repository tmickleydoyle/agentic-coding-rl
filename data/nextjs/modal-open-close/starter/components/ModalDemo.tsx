'use client'
import { useState } from 'react'

export default function ModalDemo() {
  // TODO: track open/closed state; render modal only when open with a Close button.
  return (
    <div>
      <button data-testid="open">Open</button>
    </div>
  )
}
