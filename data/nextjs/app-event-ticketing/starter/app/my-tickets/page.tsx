'use client'

export default function MyTicketsPage() {
  // TODO: list orders (order-<id> rows via OrderRow) plus ticket-count and revenue-total;
  // empty-state when there are none.
  return (
    <section data-testid="page-my-tickets">
      <h1>My Tickets</h1>
    </section>
  )
}
