'use client'

export default function CheckoutPage() {
  // TODO: render the checkout form (buyer-input, tier-select, qty-input, submit-buy) with
  // validation and sold-out handling. no-event when nothing selected.
  return (
    <section data-testid="page-checkout">
      <h1>Checkout</h1>
    </section>
  )
}
