'use client'

export default function EventDetailPage() {
  // TODO: show the selected event's tiers with remaining/price; mark sold-out tiers and
  // render a buy button for available ones. no-event when nothing selected.
  return (
    <section data-testid="page-event-detail">
      <h1>Event</h1>
    </section>
  )
}
