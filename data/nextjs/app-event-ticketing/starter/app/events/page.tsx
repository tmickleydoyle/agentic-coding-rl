'use client'

export default function EventsPage() {
  // TODO: list events (event-<id> rows via EventCard) with a view button per event.
  return (
    <section data-testid="page-events">
      <h1>Events</h1>
    </section>
  )
}
