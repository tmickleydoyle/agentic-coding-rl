'use client'

export default function TakePage() {
  // TODO: if no active quiz show no-active; else render take-title + a QuestionBlock per
  // question and a submit-quiz button.
  return <section data-testid="page-take" />
}
