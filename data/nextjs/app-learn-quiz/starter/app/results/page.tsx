'use client'

export default function ResultsPage() {
  // TODO: if not submitted show no-results; else show score-value/total-value, pass-fail
  // (data-passed), a review-button and a retake-button.
  return <section data-testid="page-results" />
}
