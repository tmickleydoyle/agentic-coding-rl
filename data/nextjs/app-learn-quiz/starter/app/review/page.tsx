'use client'

export default function ReviewPage() {
  // TODO: if not submitted show no-review; else a review-<qid> block per question with
  // chosen/correct text and data-correct.
  return <section data-testid="page-review" />
}
