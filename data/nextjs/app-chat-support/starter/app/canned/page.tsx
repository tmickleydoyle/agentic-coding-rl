'use client'

export default function CannedPage() {
  // TODO: render a canned-list of CannedRows; use- sends the canned text into the
  // selected chat (disabled when no chat is selected).
  return <section data-testid="page-canned" />
}
