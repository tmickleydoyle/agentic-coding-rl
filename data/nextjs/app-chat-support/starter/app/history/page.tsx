'use client'

export default function HistoryPage() {
  // TODO: render theme-select, support-stats (stat-total/open/closed), and a history-list
  // of all chats.
  return <section data-testid="page-history" />
}
