'use client'

export default function ChatPage() {
  // TODO: render the selected chat (chat-title, chat-status, close-toggle, assign-select,
  // reply-list, send-form). Show no-chat-selected when nothing is selected.
  return <section data-testid="page-chat" />
}
