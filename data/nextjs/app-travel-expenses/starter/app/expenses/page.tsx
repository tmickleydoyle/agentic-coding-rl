'use client'

export default function ExpensesPage() {
  // TODO: no-trip when nothing selected; otherwise expenses-name/expenses-total and a
  // day-<n> block per day with removable ExpenseRows + per-day total.
  return <section data-testid="page-expenses" />
}
