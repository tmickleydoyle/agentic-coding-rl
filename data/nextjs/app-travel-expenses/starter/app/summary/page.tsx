'use client'

export default function SummaryPage() {
  // TODO: StatCards (trips/count/grand-total), a per-trip totals list, and a
  // category breakdown for the focus trip.
  return <section data-testid="page-summary" />
}
