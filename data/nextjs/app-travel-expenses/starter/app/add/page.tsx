'use client'

export default function AddPage() {
  // TODO: form with trip-select, day-input, category-select, amount-input, note-input,
  // submit-expense. Amount <= 0 => form-error; otherwise add expense and go to expenses.
  return <section data-testid="page-add" />
}
