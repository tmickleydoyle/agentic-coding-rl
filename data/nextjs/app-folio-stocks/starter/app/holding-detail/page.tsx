'use client'

export default function HoldingDetailPage() {
  // TODO: show the selected holding's value/cost/gain-loss/allocation and a remove button,
  // or <p data-testid="no-holding-selected"> when nothing is selected.
  return <section data-testid="page-holding-detail" />
}
