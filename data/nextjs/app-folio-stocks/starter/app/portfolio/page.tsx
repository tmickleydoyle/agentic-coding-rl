'use client'

export default function PortfolioPage() {
  // TODO: render StatCards (value/cost/gainloss/percent/count), a theme toggle, and the
  // holding-list of HoldingCards (or empty-holdings when none).
  return <section data-testid="page-portfolio" />
}
