'use client'

export default function MyAgendaPage() {
  // TODO: list agenda sessions (agenda-<id> rows via AgendaRow) with a drop button plus
  // agenda-count; empty-state when the agenda is empty.
  return (
    <section data-testid="page-my-agenda">
      <h1>My Agenda</h1>
    </section>
  )
}
