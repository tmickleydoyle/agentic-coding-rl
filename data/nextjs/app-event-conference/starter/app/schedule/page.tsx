'use client'

export default function SchedulePage() {
  // TODO: render a track-filter select and list matching sessions (session-<id> rows via
  // SessionCard) with a view button; no-sessions when the filter matches nothing.
  return (
    <section data-testid="page-schedule">
      <h1>Schedule</h1>
    </section>
  )
}
