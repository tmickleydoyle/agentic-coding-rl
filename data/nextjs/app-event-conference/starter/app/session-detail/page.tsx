'use client'

export default function SessionDetailPage() {
  // TODO: show the selected session's title/speaker/slot; add-btn or remove-btn depending on
  // agenda membership; conflict-error + conflict-with when an add fails. no-session when none.
  return (
    <section data-testid="page-session-detail">
      <h1>Session</h1>
    </section>
  )
}
