'use client'

export default function SpeakersPage() {
  // TODO: list each distinct speaker (speaker-<name> rows) with speaker-<name>-count.
  return (
    <section data-testid="page-speakers">
      <h1>Speakers</h1>
    </section>
  )
}
