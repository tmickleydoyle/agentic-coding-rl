'use client'

export default function RoadmapPage() {
  // TODO: render a column per quarter (quarter-<id>) with a label, a count, and a list of
  // InitiativeCards; open-<id> selects the initiative.
  return <section data-testid="page-roadmap" />
}
