'use client'

export default function InitiativeDetailPage() {
  // TODO: show the selected initiative (no-initiative when none); detail-title,
  // detail-status, a status-select that calls setStatus, and a quarter-select that calls
  // moveInitiative.
  return <section data-testid="page-initiative-detail" />
}
