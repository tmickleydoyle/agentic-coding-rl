'use client'

export default function TimelinePage() {
  // TODO: render status rollup counts (timeline-planned/in-progress/done-count) and a
  // timeline-list of every initiative in quarter order.
  return <section data-testid="page-timeline" />
}
