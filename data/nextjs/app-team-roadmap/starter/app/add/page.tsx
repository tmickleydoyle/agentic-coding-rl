'use client'

export default function AddPage() {
  // TODO: render add-form with title-input, quarter-select, submit-initiative. Validate
  // title (form-error if blank), addInitiative, navigate('roadmap').
  return <section data-testid="page-add" />
}
