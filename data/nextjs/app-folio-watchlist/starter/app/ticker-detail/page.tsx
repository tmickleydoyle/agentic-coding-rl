'use client'

export default function TickerDetailPage() {
  // TODO: show the selected ticker's price/target/distance/alert and a remove button, or
  // <p data-testid="no-ticker-selected"> when nothing is selected.
  return <section data-testid="page-ticker-detail" />
}
