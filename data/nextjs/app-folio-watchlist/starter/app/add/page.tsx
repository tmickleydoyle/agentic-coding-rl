'use client'

export default function AddPage() {
  // TODO: render <form data-testid="ticker-form"> with symbol/name/price/target/direction
  // inputs, validate, then addTicker and navigate('watchlist').
  return <section data-testid="page-add" />
}
