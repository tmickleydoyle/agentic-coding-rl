'use client'

export default function WatchlistPage() {
  // TODO: render StatCards (count/alerts), a theme toggle, and the ticker-list of
  // TickerCards (or empty-watchlist when none).
  return <section data-testid="page-watchlist" />
}
