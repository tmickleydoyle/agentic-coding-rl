'use client'

export default function DashboardPage() {
  // TODO: render StatCards (income/monthly/count/months), a theme toggle, and the
  // holding-list of HoldingCards (or empty-holdings when none).
  return <section data-testid="page-dashboard" />
}
