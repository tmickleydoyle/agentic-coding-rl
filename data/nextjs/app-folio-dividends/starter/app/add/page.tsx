'use client'

export default function AddPage() {
  // TODO: render <form data-testid="holding-form"> with symbol/name/shares/per-share/month
  // inputs, validate, then addHolding and navigate('dashboard').
  return <section data-testid="page-add" />
}
