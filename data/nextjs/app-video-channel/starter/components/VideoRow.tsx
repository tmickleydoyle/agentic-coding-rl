'use client'
import type { Video } from '../lib/types'

export default function VideoRow(_props: {
  video: Video
  views: number
  onOpen: (id: string) => void
}) {
  // TODO: render the cv-<id> row with title, views, and an open button.
  return null
}
