'use client'

export default function ListPage() {
  // TODO: no-trip when nothing selected; otherwise list-name/list-percent and
  // category-<cat> blocks with togglable/removable ItemRows.
  return <section data-testid="page-list" />
}
