export default function ItemList({ items }: { items: string[] }) {
  // TODO: render a <ul data-testid="list"> with the items, or an empty-state element
  // <... data-testid="empty">No items</...> when items.length === 0.
  return <ul data-testid="list"></ul>
}
