'use client'

export default function AddCardPage() {
  // TODO: render add-card-form with card-title-input and submit-card. On submit: validate
  // the title (form-error if empty), addCard(...), navigate('board').
  return <section data-testid="page-add-card" />
}
