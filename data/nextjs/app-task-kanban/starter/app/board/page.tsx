'use client'

export default function BoardPage() {
  // TODO: render the three columns (backlog/doing/done) using useColumns(), each with its
  // count, optional WIP warning, and card list with move/archive/delete actions.
  return <section data-testid="page-board" />
}
