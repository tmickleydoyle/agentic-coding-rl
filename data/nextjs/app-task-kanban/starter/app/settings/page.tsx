'use client'

export default function SettingsPage() {
  // TODO: render current-theme + theme-toggle, and current-wip + wip-input + wip-save to
  // update the WIP limit in context.
  return <section data-testid="page-settings" />
}
