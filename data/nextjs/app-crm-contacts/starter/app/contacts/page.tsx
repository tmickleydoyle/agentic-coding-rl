'use client'

export default function ContactsPage() {
  // TODO: tag-filter select; contact-list of rows with company + tag counts; open-<id>
  // selects + navigates to the detail page; empty-state when nothing matches.
  return <section data-testid="page-contacts" />
}
