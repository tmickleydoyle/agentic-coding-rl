'use client'

export default function ContactDetailPage() {
  // TODO: no-contact when none selected; else name/company, tag list with remove buttons,
  // a tag-input + add-tag, and the contact's activity log.
  return <section data-testid="page-contact-detail" />
}
