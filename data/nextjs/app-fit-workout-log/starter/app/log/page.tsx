'use client'

export default function LogPage() {
  // TODO: render the log form (name/date/exercise/reps/weight + submit-workout) and the
  // workout-list of WorkoutRow items; empty-state when there are none.
  return <section data-testid="page-log" />
}
