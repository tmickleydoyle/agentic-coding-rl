'use client'

export default function PostPage() {
  // TODO: render the selected post detail (detail-text/author/likes, detail-like),
  // comment-list, and comment-form. Show no-post-selected when nothing is selected.
  return <section data-testid="page-post" />
}
