'use client'

export default function ProfilePage() {
  // TODO: render profile-name/handle/post-count, a follow-toggle (non-self only),
  // and profile-posts for the selected (or current) user.
  return <section data-testid="page-profile" />
}
