'use client'

export default function UnitDetailPage() {
  // TODO: when no unit is selected render <p data-testid="no-unit">; otherwise render
  // detail-label, detail-rent, detail-occupied, a unit-app-list, and an apply-form with
  // applicant-input + submit-application (validate, addApplication, clear input).
  return <section data-testid="page-unit-detail" />
}
