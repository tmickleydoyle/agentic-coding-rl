'use client'

export default function UnitsPage() {
  // TODO: render a unit-list of UnitCards (toggle-<id> flips occupied; open-<id> selects +
  // navigates to unit-detail).
  return <section data-testid="page-units" />
}
