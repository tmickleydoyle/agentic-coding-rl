'use client'

export default function ApplicationsPage() {
  // TODO: render an application-list of application-<id> items (applicant, unit label,
  // status) with approve-<id> / reject-<id> / pending-<id> buttons.
  return <section data-testid="page-applications" />
}
