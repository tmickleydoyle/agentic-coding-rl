'use client'

export default function CreatePage() {
  // TODO: render create-form with question-input and option-input-0/1/2 + submit-poll.
  // On submit: createPoll(...); show create-error if null, else openPoll(newId).
  return <section data-testid="page-create" />
}
