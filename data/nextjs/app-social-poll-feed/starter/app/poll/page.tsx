'use client'

export default function PollPage() {
  // TODO: render the selected poll detail (detail-question, option-list of OptionRows,
  // already-voted message when voted). Show no-poll-selected when nothing is selected.
  return <section data-testid="page-poll" />
}
