'use client'

export default function SettingsPage() {
  // TODO: render a toggle-theme button, a current-theme span, and a default-range <select>
  // bound to the shared range.
  return <section data-testid="page-settings" />
}
