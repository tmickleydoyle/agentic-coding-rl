'use client'

export default function PagesPage() {
  // TODO: render <ul data-testid="page-list"> (PageRow per page) and, when a page is
  // selected, a <div data-testid="page-detail"> with detail-path and detail-sessions.
  return <section data-testid="page-pages" />
}
