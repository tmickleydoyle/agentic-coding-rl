'use client'

export default function SourcesPage() {
  // TODO: render <ul data-testid="source-list"> with name, sessions, and conversion rate
  // (source-<id>-rate = round(conversions / sessions * 100)).
  return <section data-testid="page-sources" />
}
