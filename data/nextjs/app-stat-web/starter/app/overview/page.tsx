'use client'

export default function OverviewPage() {
  // TODO: render a range-filter <select>, StatCards (total-views/total-sessions/avg-bounce),
  // and a top-pages <ul> of the top 3 pages by range-adjusted views.
  return <section data-testid="page-overview" />
}
