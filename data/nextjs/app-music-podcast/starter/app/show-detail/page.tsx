'use client'

export default function ShowDetailPage() {
  // TODO: when nothing selected render no-show; otherwise detail-title, detail-played-count,
  // and episode-list (ep-<id> with title, played-<id>, toggle-played-<id>, enqueue-<id>).
  return <section data-testid="page-show-detail" />
}
