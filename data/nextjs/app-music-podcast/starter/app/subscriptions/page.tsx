'use client'

export default function SubscriptionsPage() {
  // TODO: render unplayed-count and subs-list (sub-<id> with title + unsub-<id>) or
  // subs-empty.
  return <section data-testid="page-subscriptions" />
}
