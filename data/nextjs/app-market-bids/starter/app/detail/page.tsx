'use client'

export default function DetailPage() {
  // TODO: show the selected auction (detail-title/bid/high), bid-form with bid-input and
  // place-bid (bid-error on failure), close-auction; closed-note + no form when closed;
  // no-selection when nothing is selected.
  return <section data-testid="page-detail" />
}
