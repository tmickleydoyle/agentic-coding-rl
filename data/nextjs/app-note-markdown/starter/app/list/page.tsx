'use client'

export default function ListPage() {
  // TODO: render a new-note button, active-filter text, and note-list of note-<id> rows
  // (title, words, edit-<id>, delete-<id>) or list-empty when nothing matches.
  return <section data-testid="page-list" />
}
