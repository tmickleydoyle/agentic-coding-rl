'use client'

export default function EditorPage() {
  // TODO: render note-form (title-input, body-input, tags-input, save-note), a live
  // preview (preview via renderMarkdown) and live-words. Prefill from editingNoteId. On
  // submit: validate title (form-error), add/update note, navigate('list').
  return <section data-testid="page-editor" />
}
