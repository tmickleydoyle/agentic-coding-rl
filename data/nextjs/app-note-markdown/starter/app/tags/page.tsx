'use client'

export default function TagsPage() {
  // TODO: render tag-list of tag-<tag> rows (tag-<tag>-count + filter-<tag> button) plus a
  // clear-filter button.
  return <section data-testid="page-tags" />
}
