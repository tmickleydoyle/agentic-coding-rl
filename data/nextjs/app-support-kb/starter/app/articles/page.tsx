'use client'

export default function ArticlesPage() {
  // TODO: render a category-filter select and the filtered article-list of ArticleRows
  // (or an empty-state when nothing matches).
  return <section data-testid="page-articles" />
}
