'use client'

export default function SearchPage() {
  // TODO: render a search-input; show search-hint before typing, no-results when nothing
  // matches, else search-results (result-<id> with an open-result-<id> button).
  return <section data-testid="page-search" />
}
