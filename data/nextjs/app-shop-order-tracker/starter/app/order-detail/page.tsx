'use client'

export default function OrderDetailPage() {
  // TODO: show the selected order detail (item/total/status) + reorder + go-track, or a
  // no-selection message.
  return <section data-testid="page-order-detail" />
}
