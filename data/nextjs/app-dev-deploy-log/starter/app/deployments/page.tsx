'use client'

export default function DeploymentsPage() {
  // TODO: render a theme-toggle + current-theme, a <Filters/>, then the env-filtered deploy
  // list (deploy-list of DeployRows) or an empty-state <p data-testid="empty-state">.
  return <section data-testid="page-deployments" />
}
