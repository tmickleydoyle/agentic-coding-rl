'use client'

export default function StatsPage() {
  // TODO: render stat-total-value, stat-success-rate-value (rounded percentage), and
  // status-count-<status>-value for every status.
  return <section data-testid="page-stats" />
}
