'use client'

export default function DeployDetailPage() {
  // TODO: if no selectedId render no-selection; else show detail-service/env/status, a
  // status timeline (stage-<stage> with data-reached), and a rollback-detail button.
  return <section data-testid="page-deploy-detail" />
}
