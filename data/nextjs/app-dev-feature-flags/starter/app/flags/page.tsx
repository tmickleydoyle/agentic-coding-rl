'use client'

export default function FlagsPage() {
  // TODO: render a theme-toggle + current-theme and a flag-list of FlagRows (key, rollout,
  // enabled count, view button).
  return <section data-testid="page-flags" />
}
