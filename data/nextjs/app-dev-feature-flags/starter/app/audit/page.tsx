'use client'

export default function AuditPage() {
  // TODO: render an audit-list (newest first) of entries (action + flag key), or audit-empty
  // when there are none.
  return <section data-testid="page-audit" />
}
