'use client'

export default function FlagDetailPage() {
  // TODO: if no selectedId render no-selection; else show detail-key, per-env toggles
  // (env-row-<env> + toggle-<env>), detail-rollout, a rollout-input and a set-rollout button.
  return <section data-testid="page-flag-detail" />
}
