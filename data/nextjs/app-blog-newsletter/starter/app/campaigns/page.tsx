'use client'

export default function CampaignsPage() {
  // TODO: render a status-filter select and a campaign-list of CampaignItems (or empty-state).
  return <section data-testid="page-campaigns" />
}
