'use client'

export default function SubscribersPage() {
  // TODO: render a subscriber-form (email-input, submit-subscriber, form-error if blank) and
  // a subscriber-list where each subscriber-<id> has data-active and a toggle-<id> button.
  return <section data-testid="page-subscribers" />
}
