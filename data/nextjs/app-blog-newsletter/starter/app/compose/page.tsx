'use client'

export default function ComposePage() {
  // TODO: render compose-form with subject-input, body-input, submit-campaign. On submit:
  // validate subject (form-error if empty), addCampaign(...), navigate('campaigns').
  return <section data-testid="page-compose" />
}
