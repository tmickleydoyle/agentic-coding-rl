'use client'

export default function DashboardPage() {
  // TODO: render StatCards (campaigns/sent/draft/subscribers/active) from useNewsletter(),
  // a current-theme, and a theme-toggle button that flips light/dark in context.
  return <section data-testid="page-dashboard" />
}
