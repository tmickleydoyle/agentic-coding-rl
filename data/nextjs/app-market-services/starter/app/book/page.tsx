'use client'

export default function BookPage() {
  // TODO: render book-form with book-name input and submit-booking. Validate name
  // (form-error if blank), book(...), navigate('bookings'). no-selection when none selected.
  return <section data-testid="page-book" />
}
