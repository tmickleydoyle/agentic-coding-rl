'use client'

export default function DetailPage() {
  // TODO: show the selected gig (detail-title/price/rating), book-this button, review-list
  // (or no-reviews), and a review-form (review-author/rating/text, submit-review) that adds
  // a review. no-selection when nothing is selected.
  return <section data-testid="page-detail" />
}
