'use client'

export default function TodayPage() {
  // TODO: render today-date (TODAY), a quick-new button, and today-list of today-<id> rows
  // (with today-<id>-mood) or today-empty when there are none.
  return <section data-testid="page-today" />
}
