'use client'

export default function EntriesPage() {
  // TODO: render a mood-filter select and entry-list of entry-<id> rows (date, body, a
  // mood-<id> cycle button, delete-<id>) newest-first, or entries-empty when none match.
  return <section data-testid="page-entries" />
}
