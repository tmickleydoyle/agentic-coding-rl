'use client'

export default function NewEntryPage() {
  // TODO: render entry-form (date-input prefilled with TODAY, body-input, mood-select,
  // save-entry). On submit: validate body (form-error), addEntry, navigate('entries').
  return <section data-testid="page-new" />
}
