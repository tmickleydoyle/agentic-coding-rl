'use client'

export default function TicketDetailPage() {
  // TODO: if no ticket selected render <p data-testid="no-selection">; otherwise show
  // detail-subject/requester/priority/status/assignee, an assignee-input + assign-btn,
  // a status-select, the transcript, and a reply-form (reply-input + reply-btn).
  return <section data-testid="page-ticket-detail" />
}
