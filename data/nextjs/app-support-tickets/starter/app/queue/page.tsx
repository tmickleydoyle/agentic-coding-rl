'use client'

export default function QueuePage() {
  // TODO: render status-count StatCards plus status/priority/assignee filters and the
  // filtered queue-list (or an empty-state when nothing matches).
  return <section data-testid="page-queue" />
}
