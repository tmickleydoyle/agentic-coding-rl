'use client'

export default function NewTicketPage() {
  // TODO: render new-ticket-form with subject-input, requester-input, priority-select and a
  // submit-ticket button; show form-error when subject is blank, else addTicket + navigate.
  return <section data-testid="page-new" />
}
