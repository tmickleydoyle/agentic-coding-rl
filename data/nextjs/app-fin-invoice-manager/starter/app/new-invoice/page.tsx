'use client'

export default function NewInvoicePage() {
  // TODO: render a new-invoice-form (client-select, amount-input, due-input, submit-invoice);
  // on submit, validate amount > 0 (else form-error) then addInvoice and navigate('invoices').
  return <section data-testid="page-new-invoice" />
}
