'use client'

export default function InvoicesPage() {
  // TODO: render <Filters/> then the filtered invoice list (invoice-list of InvoiceRows) or
  // an empty-state <p data-testid="empty-state"> when nothing matches.
  return <section data-testid="page-invoices" />
}
