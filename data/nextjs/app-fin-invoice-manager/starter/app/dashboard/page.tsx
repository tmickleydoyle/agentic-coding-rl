'use client'

export default function DashboardPage() {
  // TODO: read stats from useInvoiceStats(); render StatCards (total/outstanding/paid/overdue),
  // per-status counts (status-count-<status>-value), and a theme-toggle + current-theme.
  return <section data-testid="page-dashboard" />
}
