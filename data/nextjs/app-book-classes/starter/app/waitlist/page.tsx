'use client'

export default function WaitlistPage() {
  // TODO: list waitlisted enrollments (class/student + cancel); waitlist-empty when none.
  return (
    <section data-testid="page-waitlist">
      <h1>Waitlist</h1>
    </section>
  )
}
