'use client'

export default function AccountDetailPage() {
  // TODO: if no account is selected, render no-selection. Otherwise show account-name,
  // account-balance, the deposit/withdrawal/count stats, and the txn-list.
  return <section data-testid="page-account-detail" />
}
