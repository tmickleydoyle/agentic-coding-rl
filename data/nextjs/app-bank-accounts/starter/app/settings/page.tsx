'use client'

export default function SettingsPage() {
  // TODO: render current-theme, a theme-toggle button, and a currency-select bound to
  // currency. Theme should persist on app-root's data-theme.
  return <section data-testid="page-settings" />
}
