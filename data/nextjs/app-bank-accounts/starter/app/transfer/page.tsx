'use client'

export default function TransferPage() {
  // TODO: render transfer-form with from-select, to-select, amount-input, submit-transfer.
  // On submit call transfer(...); show transfer-error on failure or transfer-success on ok.
  return <section data-testid="page-transfer" />
}
