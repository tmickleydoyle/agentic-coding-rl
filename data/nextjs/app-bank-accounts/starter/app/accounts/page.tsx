'use client'

export default function AccountsPage() {
  // TODO: render currency-label, total-balance, and an account-list of AccountCards
  // (or empty-accounts when there are none).
  return <section data-testid="page-accounts" />
}
