'use client'

export default function ThreadPage() {
  // TODO: render the selected thread (thread-title, dm-list, send-form, mark-unread).
  // Show no-thread-selected when nothing is selected.
  return <section data-testid="page-thread" />
}
