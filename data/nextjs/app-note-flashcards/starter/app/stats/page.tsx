'use client'

export default function StatsPage() {
  // TODO: render stat-<id> rows with stat-<id>-known, stat-<id>-total, and a reset-<id>
  // button per deck.
  return <section data-testid="page-stats" />
}
