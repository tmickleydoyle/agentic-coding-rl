'use client'

export default function StudyPage() {
  // TODO: when no deck selected render no-deck; otherwise render study-progress and the
  // study-card (card-face, flip-card, mark-known, mark-unknown, next-card) or study-empty.
  return <section data-testid="page-study" />
}
