'use client'

export default function AddPage() {
  // TODO: when no deck selected render no-deck; otherwise render card-form (front-input,
  // back-input, save-card). On submit: validate both (form-error), addCard, navigate('study').
  return <section data-testid="page-add" />
}
