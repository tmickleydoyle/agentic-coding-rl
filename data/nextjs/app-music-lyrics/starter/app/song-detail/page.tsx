'use client'

export default function SongDetailPage() {
  // TODO: when nothing selected render no-song; otherwise detail-title/artist, view-artist,
  // and line-list (line-<index> with line-<index>-text + fav-line-<index>).
  return <section data-testid="page-song-detail" />
}
