'use client'

export default function SearchPage() {
  // TODO: render search-input and search-results (sresult-<songId>-<lineIndex> with -line
  // and -title spans) or search-empty.
  return <section data-testid="page-search" />
}
