'use client'

export default function FavoritesPage() {
  // TODO: render fav-count and fav-list (favline-<songId>-<lineIndex> with -text/-song spans
  // + remove-<songId>-<lineIndex>) or fav-empty.
  return <section data-testid="page-favorites" />
}
