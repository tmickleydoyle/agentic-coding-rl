'use client'

export default function RegisterPage() {
  // TODO: when no house is selected render <p data-testid="no-house">; otherwise render
  // register-form with name-input and submit-visitor. On submit: validate name (form-error
  // if empty), registerVisitor(...), navigate('house-detail').
  return <section data-testid="page-register" />
}
