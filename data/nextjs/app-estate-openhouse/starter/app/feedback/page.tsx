'use client'

export default function FeedbackPage() {
  // TODO: when no house is selected render <p data-testid="no-house">; otherwise render
  // feedback-form with visitor-input, rating-input, note-input, submit-feedback. On submit:
  // validate visitor (form-error if empty), addFeedback(...), navigate('house-detail').
  return <section data-testid="page-feedback" />
}
