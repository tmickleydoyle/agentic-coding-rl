'use client'

export default function HouseDetailPage() {
  // TODO: when no house is selected render <p data-testid="no-house">; otherwise render
  // detail-address, detail-time, detail-count, detail-avg, a visitor-list and a feedback-list.
  return <section data-testid="page-house-detail" />
}
