'use client'

export default function SchedulePage() {
  // TODO: render total-visitors and a house-list of HouseCards (open-<id> selects + navigates).
  return <section data-testid="page-schedule" />
}
