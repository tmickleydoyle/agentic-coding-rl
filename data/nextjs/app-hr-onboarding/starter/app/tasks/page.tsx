'use client'

export default function TasksPage() {
  // TODO: render task-list of all tasks with hire name and a toggle button; tasks-total
  // and tasks-done spans show the counts.
  return <section data-testid="page-tasks" />
}
