'use client'

export default function SettingsPage() {
  // TODO: render StatCards (total/pending/approved/spam) from useComments(), a current-theme,
  // and a theme-toggle button that flips light/dark in context.
  return <section data-testid="page-settings" />
}
