'use client'

export default function ModerationPage() {
  // TODO: render a status-filter select and a comment-list of all (filtered) comments as
  // CommentItems, or an empty-state when nothing matches.
  return <section data-testid="page-moderation" />
}
