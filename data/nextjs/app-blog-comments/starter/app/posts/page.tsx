'use client'

export default function PostsPage() {
  // TODO: render a post-list where each post-<id> shows the title, a per-post comment
  // count (post-<id>-count), and an open-<id> button calling openPost(id).
  return <section data-testid="page-posts" />
}
