'use client'

export default function AlbumDetailPage() {
  // TODO: when nothing selected render no-album; otherwise detail-title/artist/year/rating/
  // total-length, rate-1..rate-5, toggle-fav, and track-list (track-<id> title + length).
  return <section data-testid="page-album-detail" />
}
