'use client'

export default function FavoritesPage() {
  // TODO: render avg-rating and favorites-list (favorite-<id> with title + unfav-<id>) or
  // favorites-empty.
  return <section data-testid="page-favorites" />
}
