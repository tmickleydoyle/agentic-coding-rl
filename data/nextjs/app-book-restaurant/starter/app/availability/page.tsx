'use client'

export default function AvailabilityPage() {
  // TODO: render time-select + party-input and list available tables (avail-<id>), or
  // none-available when nothing fits.
  return (
    <section data-testid="page-availability">
      <h1>Availability</h1>
    </section>
  )
}
