'use client'

export default function ReservePage() {
  // TODO: render the reserve form (name-input, table-select, time-select, party-input,
  // submit-reserve) with validation and conflict handling.
  return (
    <section data-testid="page-reserve">
      <h1>Reserve</h1>
    </section>
  )
}
