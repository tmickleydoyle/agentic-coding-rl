'use client'

export default function MyTradesPage() {
  // TODO: list offers made by ME (mytrades-list / mytrade-<id> with item + status);
  // no-trades when empty.
  return <section data-testid="page-mytrades" />
}
