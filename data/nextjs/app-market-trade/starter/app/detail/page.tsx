'use client'

export default function DetailPage() {
  // TODO: show the selected item (detail-name/owner), its offers (offer-list / offer-<id>
  // with data-status; accept-<id>/decline-<id> only when pending; no-offers when none), and
  // a propose-form (give-input, submit-offer; form-error on blank). no-selection when none.
  return <section data-testid="page-detail" />
}
