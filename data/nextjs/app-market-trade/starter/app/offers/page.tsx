'use client'

export default function OffersPage() {
  // TODO: render a status-filter <select> then all-offers-list (alloffer-<id>) of offers
  // matching the filter; no-matching when none match.
  return <section data-testid="page-offers" />
}
