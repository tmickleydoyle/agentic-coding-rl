'use client'

export default function BuildsPage() {
  // TODO: render a status-filter <select> then the filtered all-build-list (row-<id>) or
  // an empty-state <p data-testid="empty-state"> when nothing matches.
  return <section data-testid="page-builds" />
}
