'use client'

export default function PipelinesPage() {
  // TODO: render <ul data-testid="pipeline-list"> with one row per pipeline (name, repo,
  // and a select-<id> button that calls selectPipeline(id)).
  return <section data-testid="page-pipelines" />
}
