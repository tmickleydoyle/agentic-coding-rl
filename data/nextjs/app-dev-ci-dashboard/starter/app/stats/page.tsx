'use client'

export default function StatsPage() {
  // TODO: render StatCards (total/passing/failing/running/success-rate) and per-pipeline
  // build counts (pipeline-builds-<id>-value).
  return <section data-testid="page-stats" />
}
