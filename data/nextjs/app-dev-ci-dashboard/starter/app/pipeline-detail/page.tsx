'use client'

export default function PipelineDetailPage() {
  // TODO: if no pipeline selected render <p data-testid="no-selection">; otherwise show
  // detail-name and a build-list of that pipeline's builds (BuildRow with retry).
  return <section data-testid="page-pipeline-detail" />
}
