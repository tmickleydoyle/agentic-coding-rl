'use client'

export default function PropertiesPage() {
  // TODO: render a property-list of <li property-<id>> with address and the count of
  // leads assigned to each property (property-<id>-leads).
  return <section data-testid="page-properties" />
}
