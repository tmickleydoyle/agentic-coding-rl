'use client'

export default function LeadsPage() {
  // TODO: render <StatusFilter/>, lead-count, and a lead-list of LeadRows (open-<id>), or
  // an empty-state when nothing matches.
  return <section data-testid="page-leads" />
}
