'use client'

export default function LeadDetailPage() {
  // TODO: show the selectedId lead (detail-name/status/property), a status-select, an
  // assign-select and a detail-back button; detail-empty when none selected.
  return <section data-testid="page-lead-detail" />
}
