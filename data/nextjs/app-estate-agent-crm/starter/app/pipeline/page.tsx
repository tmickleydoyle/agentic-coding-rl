'use client'

export default function PipelinePage() {
  // TODO: render a pipeline with one stage-<status> column per status (count + leads), a
  // current-theme <p> and a theme-toggle button.
  return <section data-testid="page-pipeline" />
}
