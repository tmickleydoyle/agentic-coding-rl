'use client'

export default function CategoriesPage() {
  // TODO: read counts from usePosts(); render StatCards (total/published/draft) and
  // per-category counts (category-count-<id>-value).
  return <section data-testid="page-categories" />
}
