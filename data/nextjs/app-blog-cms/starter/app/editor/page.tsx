'use client'

export default function EditorPage() {
  // TODO: render editor-form with title-input, body-input, category-select, status-select,
  // submit-post. On submit: validate title (form-error if empty), addPost(...), navigate('posts').
  return <section data-testid="page-editor" />
}
