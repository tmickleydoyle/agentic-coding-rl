'use client'

export default function PublishedPage() {
  // TODO: render current-theme + theme-toggle, a published-count, and a published-list
  // of only published posts.
  return <section data-testid="page-published" />
}
