'use client'

export default function PostsPage() {
  // TODO: render <Filters/> then the filtered post-list of PostItems, or an
  // empty-state <p data-testid="empty-state"> when nothing matches.
  return <section data-testid="page-posts" />
}
