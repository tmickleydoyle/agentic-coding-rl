'use client'

export default function PipelinePage() {
  // TODO: render a column per stage (column-applied/screen/interview/offer/hired) with
  // counts and CandidateCards; the advance button moves a candidate forward.
  return <section data-testid="page-pipeline" />
}
