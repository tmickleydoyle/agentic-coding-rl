'use client'

export default function JobsPage() {
  // TODO: render job-list of JobRows with per-job candidate counts; open-<id> selects the
  // job and navigates to the detail page.
  return <section data-testid="page-jobs" />
}
