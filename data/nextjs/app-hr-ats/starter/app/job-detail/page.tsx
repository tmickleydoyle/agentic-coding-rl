'use client'

export default function JobDetailPage() {
  // TODO: show the selected job (no-job message when none); list its candidates with
  // names, stages, and a detail-count.
  return <section data-testid="page-job-detail" />
}
