'use client'

export default function CandidatesPage() {
  // TODO: render candidate-list; each candidate-<id> shows name, job title, and a
  // stage-<id> select that calls moveStage; set data-stage on the row.
  return <section data-testid="page-candidates" />
}
