'use client'

export default function SessionPage() {
  // TODO: if no session selected render <p data-testid="no-selection">; otherwise show
  // detail-visitor/topic/status/agent, an agent-select + assign-btn, a close-btn, the
  // transcript, and a message-form (message-input + send-btn).
  return <section data-testid="page-session" />
}
