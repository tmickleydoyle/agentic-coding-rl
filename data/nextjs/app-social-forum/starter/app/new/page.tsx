'use client'

export default function NewThreadPage() {
  // TODO: render new-thread-form with title-input, category-select, submit-thread.
  // On submit: validate title (form-error if empty), addThread(...), navigate('threads').
  return <section data-testid="page-new" />
}
