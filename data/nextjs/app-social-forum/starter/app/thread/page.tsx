'use client'

export default function ThreadPage() {
  // TODO: render the selected thread detail (detail-title/votes, detail-upvote),
  // reply-list, and reply-form. Show no-thread-selected when nothing is selected.
  return <section data-testid="page-thread" />
}
