'use client'

export default function StatsPage() {
  // TODO: show total-sessions (sum), completed-tasks (count of done), and a per-task
  // breakdown list of stat-<id> with stat-<id>-sessions.
  return <section data-testid="page-stats" />
}
