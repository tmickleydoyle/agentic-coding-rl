'use client'

export default function TasksPage() {
  // TODO: render add-task-form (task-title-input/submit-task, form-error on empty) and the
  // task-list of TaskRows with focus/done/remove actions.
  return <section data-testid="page-tasks" />
}
