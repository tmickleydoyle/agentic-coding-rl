'use client'

export default function FocusPage() {
  // TODO: show focus-task (or no-task) and a Timer wired to useTimer(SESSION_SECONDS);
  // on completion call completeSession(selectedId) and reset.
  return <section data-testid="page-focus" />
}
