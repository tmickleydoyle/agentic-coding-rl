'use client'

export default function AddPage() {
  // TODO: render add-form with title-input, owner-input, submit-objective. Validate title
  // (form-error if blank), addObjective, navigate('objectives').
  return <section data-testid="page-add" />
}
