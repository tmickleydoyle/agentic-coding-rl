'use client'

export default function DashboardPage() {
  // TODO: render company-progress (rollup average), objective-count, and a dashboard-list
  // repeating each objective's rolled-up progress.
  return <section data-testid="page-dashboard" />
}
