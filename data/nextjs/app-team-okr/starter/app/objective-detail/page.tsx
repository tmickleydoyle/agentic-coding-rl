'use client'

export default function ObjectiveDetailPage() {
  // TODO: show the selected objective (no-objective when none); detail-title,
  // detail-progress, and a kr-list of key results each with a range input that calls
  // updateProgress.
  return <section data-testid="page-objective-detail" />
}
