'use client'

export default function BalancesPage() {
  // TODO: render a balance-list (per person net + data-status) and a settlement-list of
  // suggested transfers, or an all-settled <p> when no transfers are needed.
  return <section data-testid="page-balances" />
}
