'use client'

export default function DashboardPage() {
  // TODO: render StatCards (total/people/expenses/perhead) from context + useBalances(),
  // plus current-theme and a theme-toggle button.
  return <section data-testid="page-dashboard" />
}
