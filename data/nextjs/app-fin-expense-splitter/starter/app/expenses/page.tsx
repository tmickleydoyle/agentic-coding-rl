'use client'

export default function ExpensesPage() {
  // TODO: render expense-form (description-input, amount-input, payer-select, submit-expense)
  // with validation, then an expense-list of ExpenseItems or an empty-expenses <p>.
  return <section data-testid="page-expenses" />
}
