'use client'

export default function SchedulePage() {
  // TODO: render a row per slot with a count and the bookings in that slot.
  return (
    <section data-testid="page-schedule">
      <h1>Schedule</h1>
    </section>
  )
}
