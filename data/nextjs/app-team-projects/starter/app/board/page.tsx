'use client'

export default function BoardPage() {
  // TODO: render three columns (column-todo/doing/done) with counts and TaskCards; the
  // advance button moves a task forward in status.
  return <section data-testid="page-board" />
}
