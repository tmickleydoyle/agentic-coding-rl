'use client'

export default function ProjectDetailPage() {
  // TODO: show the selected project (no-project message when none); list its tasks with
  // assignee names, data-status, and a reassign-<id> select to change the assignee.
  return <section data-testid="page-project-detail" />
}
