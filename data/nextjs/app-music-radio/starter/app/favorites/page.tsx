'use client'

export default function FavoritesPage() {
  // TODO: render favorites-list (favorite-<id> with name + unfav-<id>) or favorites-empty.
  return <section data-testid="page-favorites" />
}
