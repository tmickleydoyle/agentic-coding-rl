'use client'

export default function HistoryPage() {
  // TODO: render total-plays, clear-history, and history-list (hist-<id> with name) or
  // history-empty.
  return <section data-testid="page-history" />
}
