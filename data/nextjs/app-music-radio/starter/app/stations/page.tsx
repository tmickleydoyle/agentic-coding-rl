'use client'

export default function StationsPage() {
  // TODO: render genre-filter select and station-list (station-<id> with name/genre/
  // play-<id>/fav-<id>/open-<id>) or stations-empty.
  return <section data-testid="page-stations" />
}
