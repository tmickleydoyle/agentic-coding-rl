'use client'

export default function FocusListPage() {
  // TODO: list only the do-quadrant tasks as focus-<id> in a focus-list (or empty-focus),
  // with a focus-count.
  return <section data-testid="page-focus-list" />
}
