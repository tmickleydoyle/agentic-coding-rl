'use client'

export default function AddPage() {
  // TODO: render add-form with title-input, urgent-checkbox, important-checkbox and
  // submit-task. On submit: validate the title (form-error if empty), addTask(...),
  // navigate('matrix').
  return <section data-testid="page-add" />
}
