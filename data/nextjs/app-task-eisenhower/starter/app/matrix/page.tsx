'use client'

export default function MatrixPage() {
  // TODO: render the four QuadrantBoxes (do/schedule/delegate/delete) via useQuadrants(),
  // each with its count and task list (move + delete actions).
  return <section data-testid="page-matrix" />
}
