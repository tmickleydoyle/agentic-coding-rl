'use client'

export default function CreatePage() {
  // TODO: render a create-form (name-input, date-input, submit-create); form-error on a
  // blank name, otherwise addEvent and navigate to events.
  return (
    <section data-testid="page-create">
      <h1>Create Event</h1>
    </section>
  )
}
