'use client'

export default function ResponsesPage() {
  // TODO: show the selected event's name, headcount, status tally, and invite list
  // (invite-<id> rows via InviteRow) with edit buttons. no-event when none selected.
  return (
    <section data-testid="page-responses">
      <h1>Responses</h1>
    </section>
  )
}
