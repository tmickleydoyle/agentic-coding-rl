'use client'

export default function EventsPage() {
  // TODO: list events (event-<id> rows via EventCard) with headcount + a view button, plus
  // total-headcount across all events.
  return (
    <section data-testid="page-events">
      <h1>Events</h1>
    </section>
  )
}
