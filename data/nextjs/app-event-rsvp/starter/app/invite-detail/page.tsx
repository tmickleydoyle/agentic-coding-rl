'use client'

export default function InviteDetailPage() {
  // TODO: show the selected invite's guest and an rsvp-form (status-select, extra-input,
  // submit-rsvp); call respond on submit, form-error on failure. no-invite when none.
  return (
    <section data-testid="page-invite-detail">
      <h1>Invite</h1>
    </section>
  )
}
