'use client'

export default function DealDetailPage() {
  // TODO: show no-deal when none selected; otherwise title/value/stage/contact and a
  // detail-stage-select to move the deal across stages.
  return <section data-testid="page-deal-detail" />
}
