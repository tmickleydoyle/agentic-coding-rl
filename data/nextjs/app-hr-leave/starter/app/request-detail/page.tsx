'use client'

export default function RequestDetailPage() {
  // TODO: show the selected request (no-request message when none) with employee, day,
  // days, reason, status and detail-approve/detail-reject buttons (disabled once decided).
  return <section data-testid="page-request-detail" />
}
