'use client'

export default function BalancesPage() {
  // TODO: render balance-list with each employee's allowance, used (approved days), and
  // remaining.
  return <section data-testid="page-balances" />
}
