'use client'

export default function CheckoutPage() {
  // TODO: read-only summary (count/subtotal/tax/total) + place-order that clears the cart
  // and confirms — or a checkout-empty message.
  return <section data-testid="page-checkout" />
}
