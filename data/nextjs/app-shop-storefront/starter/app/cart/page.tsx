'use client'

export default function CartPage() {
  // TODO: render cart lines with qty inputs + remove, totals (subtotal/tax/total), and a
  // go-checkout button — or a cart-empty message.
  return <section data-testid="page-cart" />
}
