'use client'

export default function SettingsPage() {
  // TODO: render current-theme, a theme-toggle button that flips light/dark, and a
  // currency-select bound to the context currency.
  return <section data-testid="page-settings" />
}
