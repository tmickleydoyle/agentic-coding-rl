'use client'

export default function AddExpensePage() {
  // TODO: render expense-form with category-select, amount-input, note-input, submit-expense.
  // On submit: validate amount (form-error if empty/non-positive), addExpense(...),
  // navigate('overview').
  return <section data-testid="page-add-expense" />
}
