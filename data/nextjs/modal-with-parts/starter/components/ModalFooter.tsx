'use client'
// TODO: render <div data-testid="modal-footer"> with <button data-testid="modal-confirm">OK</button>
// and <button data-testid="modal-cancel">Cancel</button>, each wired to its callback.
export default function ModalFooter({
  onConfirm,
  onCancel,
}: {
  onConfirm: () => void
  onCancel: () => void
}) {
  return <div />
}
