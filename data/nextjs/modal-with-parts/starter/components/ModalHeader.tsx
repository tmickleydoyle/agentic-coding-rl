'use client'
// TODO: render <div data-testid="modal-header"> with <h2 data-testid="modal-title">
// and a <button data-testid="modal-close"> that calls onClose.
export default function ModalHeader({
  title,
  onClose,
}: {
  title: string
  onClose: () => void
}) {
  return <div />
}
