'use client'
import type { Course } from '../lib/types'

export default function CourseRow(_props: {
  course: Course
  lessonCount: number
  percent: number
  onOpen: (id: string) => void
}) {
  // TODO: render the course-<id> row with title, lesson count, percent, open button.
  return null
}
