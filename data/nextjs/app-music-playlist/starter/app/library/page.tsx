'use client'

export default function LibraryPage() {
  // TODO: render song-list (song-<id> with title/artist/enqueue-<id>/play-<id>) and
  // playlist-list (playlist-<id> with name/open-playlist-<id>).
  return <section data-testid="page-library" />
}
