'use client'

export default function QueuePage() {
  // TODO: render shuffle-toggle, queue-duration, and queue-list (q-song-<id> with title +
  // remove-q-<id>) or queue-empty.
  return <section data-testid="page-queue" />
}
