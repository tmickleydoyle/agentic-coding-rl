'use client'

export default function PlaylistPage() {
  // TODO: when nothing selected render no-playlist; otherwise playlist-title, add-song-select,
  // and playlist-songs (pl-song-<id> with title + remove-<id>) or playlist-empty.
  return <section data-testid="page-playlist" />
}
