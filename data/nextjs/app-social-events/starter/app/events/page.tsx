'use client'

export default function EventsPage() {
  // TODO: render a theme-toggle + current-theme, a <Filters/>, then the time-filtered event
  // list (event-list of EventCards) or an empty-state <p data-testid="empty-state">.
  return <section data-testid="page-events" />
}
