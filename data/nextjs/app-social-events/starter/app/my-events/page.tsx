'use client'

export default function MyEventsPage() {
  // TODO: show rsvp-count-<rsvp>-value totals and a my-list of responded events (title + rsvp),
  // or my-empty when there are none.
  return <section data-testid="page-my-events" />
}
