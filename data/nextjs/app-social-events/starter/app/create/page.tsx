'use client'

export default function CreatePage() {
  // TODO: render an event-form (title-input, day-input, submit-event); on submit, validate a
  // non-empty title (else form-error) then addEvent and navigate('events').
  return <section data-testid="page-create" />
}
