'use client'

export default function EventDetailPage() {
  // TODO: if no selectedId render no-selection; else show detail-title, detail-count,
  // detail-rsvp (or 'none'), and rsvp-going/rsvp-maybe/rsvp-no buttons.
  return <section data-testid="page-event-detail" />
}
