'use client'

export default function BillDetailPage() {
  // TODO: if no bill is selected, render no-selection. Otherwise show bill-name/amount/due/
  // status, a pay-button (or already-paid), and an autopay-toggle + autopay-state.
  return <section data-testid="page-bill-detail" />
}
