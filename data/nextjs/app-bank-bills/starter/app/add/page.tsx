'use client'

export default function AddPage() {
  // TODO: render bill-form (name-input, amount-input, dueday-input, autopay-checkbox,
  // submit-bill) with validation (form-error). On success addBill(...) and navigate('bills').
  return <section data-testid="page-add" />
}
