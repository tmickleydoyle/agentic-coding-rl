'use client'

export default function UpcomingPage() {
  // TODO: render an upcoming-list (sorted by dueDay) and an overdue-list, with
  // empty-upcoming / empty-overdue fallbacks.
  return <section data-testid="page-upcoming" />
}
