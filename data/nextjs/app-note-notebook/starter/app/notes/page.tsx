'use client'

export default function NotesPage() {
  // TODO: when no notebook is selected render no-notebook; otherwise render a tag-filter
  // select, a new-note button, and the note-list (or notes-empty when nothing matches).
  return <section data-testid="page-notes" />
}
