'use client'

export default function EditorPage() {
  // TODO: render note-form with title-input, body-input, tags-input, save-note. Prefill
  // from editingNoteId when set. On submit: validate title (form-error if empty),
  // add/update the note, then navigate('notes').
  return <section data-testid="page-editor" />
}
