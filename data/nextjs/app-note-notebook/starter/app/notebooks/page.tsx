'use client'

export default function NotebooksPage() {
  // TODO: render notebook-list of notebook-<id> rows with name, count, and an open-<id>
  // button that calls selectNotebook(id).
  return <section data-testid="page-notebooks" />
}
