'use client'

export default function SearchPage() {
  // TODO: render a search-input bound to searchQuery and search-results (result-<id>), or
  // search-empty when the query is blank.
  return <section data-testid="page-search" />
}
