'use client'

export default function HistoryPage() {
  // TODO: render a date-select bound to selectedDate, a history-count, and a history-list
  // of EntryCards for the selected date (or history-empty).
  return <section data-testid="page-history" />
}
