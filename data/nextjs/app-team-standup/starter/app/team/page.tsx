'use client'

export default function TeamPage() {
  // TODO: render team-list with each member's name and total entry count
  // (team-member-<id>-count).
  return <section data-testid="page-team" />
}
