'use client'

export default function AddEntryPage() {
  // TODO: render add-form with member-select, yesterday-input, today-input, blocker-input,
  // submit-entry. Validate yesterday/today (form-error if blank), addEntry, navigate('today').
  return <section data-testid="page-add-entry" />
}
