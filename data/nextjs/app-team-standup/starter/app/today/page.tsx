'use client'

export default function TodayPage() {
  // TODO: show today's entries (today-list of EntryCards or today-empty) and a
  // today-blocker-count of today's entries that have a blocker.
  return <section data-testid="page-today" />
}
