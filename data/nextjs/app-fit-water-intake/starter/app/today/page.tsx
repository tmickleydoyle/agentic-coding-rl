'use client'

export default function TodayPage() {
  // TODO: show today's total/goal/percent/remaining/met, quick-add buttons, an add-form,
  // and today's drink-list (empty-state when none).
  return <section data-testid="page-today" />
}
