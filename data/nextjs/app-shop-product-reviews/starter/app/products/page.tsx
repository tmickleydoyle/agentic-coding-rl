'use client'

export default function ProductsPage() {
  // TODO: render a product-list with name, avg (toFixed(1)), count, a view-<id> button, plus
  // a theme-toggle and current-theme.
  return <section data-testid="page-products" />
}
