'use client'

export default function ProductReviewsPage() {
  // TODO: if no selectedId render no-selection; else show selected-name, selected-avg, a
  // sort-select, and a review-list of ReviewItems (or empty-reviews when none).
  return <section data-testid="page-product-reviews" />
}
