'use client'

export default function WriteReviewPage() {
  // TODO: render a review-form (product-select, rating-input, text-input, submit-review);
  // validate rating 1-5 and non-empty text (else form-error), then addReview + select product.
  return <section data-testid="page-write-review" />
}
