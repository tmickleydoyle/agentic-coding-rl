'use client'

export default function LowStockPage() {
  // TODO: show current-theme, a theme-toggle, a low-count line, and the low-list (or an
  // all-stocked message).
  return <section data-testid="page-low-stock" />
}
