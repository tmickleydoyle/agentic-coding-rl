'use client'

export default function ProductsPage() {
  // TODO: render stat cards, a stock-filter select, then the product-list (ProductRows)
  // or an empty-state.
  return <section data-testid="page-products" />
}
