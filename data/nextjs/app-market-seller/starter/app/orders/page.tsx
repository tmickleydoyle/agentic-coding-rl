'use client'

export default function OrdersPage() {
  // TODO: render order-list; each order shows product/qty; pending orders get a fulfill-<id>
  // button, fulfilled orders get an order-<id>-done marker.
  return <section data-testid="page-orders" />
}
