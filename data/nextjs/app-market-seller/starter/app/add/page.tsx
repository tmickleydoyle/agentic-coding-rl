'use client'

export default function AddPage() {
  // TODO: render add-form with name/price/stock inputs and submit-product. Validate name
  // (form-error if blank), addProduct(...), navigate('products').
  return <section data-testid="page-add" />
}
