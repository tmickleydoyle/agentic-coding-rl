'use client'

export default function RevenuePage() {
  // TODO: render total-revenue, pending-count, and revenue-breakdown (rev-<productId>-value
  // per product) from useSeller().
  return <section data-testid="page-revenue" />
}
