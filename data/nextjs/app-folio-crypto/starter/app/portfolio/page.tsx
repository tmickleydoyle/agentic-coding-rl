'use client'

export default function PortfolioPage() {
  // TODO: render StatCards (value/change/percent/count), a theme toggle, and the coin-list
  // of CoinCards (or empty-coins when none).
  return <section data-testid="page-portfolio" />
}
