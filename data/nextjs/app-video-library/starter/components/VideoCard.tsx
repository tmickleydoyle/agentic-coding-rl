'use client'
import type { Video } from '../lib/types'

export default function VideoCard(_props: {
  video: Video
  watched: boolean
  inList: boolean
  onOpen: (id: string) => void
}) {
  // TODO: render the video-<id> row with title, duration, badges, and an open button.
  return null
}
