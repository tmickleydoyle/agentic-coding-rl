'use client'

export default function ChatDetailPage() {
  // TODO: render the selected group (detail-name, detail-admin, member-list, add-select +
  // add-submit, leave-group for non-admins). Show no-group-selected when nothing is selected.
  return <section data-testid="page-chat-detail" />
}
