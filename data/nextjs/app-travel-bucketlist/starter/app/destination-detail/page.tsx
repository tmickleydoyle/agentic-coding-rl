'use client'
export default function DestinationDetailPage() {
  // TODO: show selected destination (name/country/continent/notes/visited) + toggle; else no-selection
  return <section data-testid="page-destination-detail" />
}
