'use client'
export default function ListPage() {
  // TODO: counts (total/visited/remaining), continent filter <select>, grouped DestinationCards, empty-state
  return <section data-testid="page-list" />
}
