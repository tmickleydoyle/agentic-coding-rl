'use client'
export default function AddDestinationPage() {
  // TODO: add-destination form (name/country/continent/notes), validation, submit -> addDestination + navigate('list')
  return <section data-testid="page-add" />
}
