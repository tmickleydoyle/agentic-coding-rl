'use client'

export default function NavBar() {
  // TODO: render nav buttons nav-list / nav-visited / nav-add / nav-destination-detail,
  // each calling navigate() and setting aria-current="page" for the active route.
  return <nav data-testid="navbar" />
}
