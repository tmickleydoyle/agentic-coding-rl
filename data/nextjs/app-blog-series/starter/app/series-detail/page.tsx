'use client'

export default function SeriesDetailPage() {
  // TODO: when no series is selected render <p data-testid="no-series">; otherwise render
  // detail-title, detail-progress, and a part-list of PartRows (toggleRead / open-reader).
  return <section data-testid="page-series-detail" />
}
