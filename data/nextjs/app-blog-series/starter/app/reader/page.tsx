'use client'

export default function ReaderPage() {
  // TODO: when no series is selected render <p data-testid="no-series">; otherwise render
  // reader-progress ("read X of Y") and a reader-list of reader-part-<id> items.
  return <section data-testid="page-reader" />
}
