'use client'

export default function AddPartPage() {
  // TODO: render add-part-form with series-select, title-input, submit-part. On submit:
  // validate title (form-error if empty), addPart(...), selectSeries, navigate('series-detail').
  return <section data-testid="page-add-part" />
}
