'use client'

export default function SeriesPage() {
  // TODO: list series via SeriesCard with part-count-<id> and progress-<id> (read percent);
  // open-<id> selects the series and navigates to series-detail.
  return <section data-testid="page-series" />
}
