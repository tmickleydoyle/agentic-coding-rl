'use client'

export default function PortfolioPage() {
  // TODO: render StatCards (value/target-total/count/balanced), a theme toggle, and the
  // holding-list of HoldingRows (or empty-holdings when none).
  return <section data-testid="page-portfolio" />
}
