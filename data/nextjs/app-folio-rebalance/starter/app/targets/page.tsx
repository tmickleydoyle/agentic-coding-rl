'use client'

export default function TargetsPage() {
  // TODO: render the target total + validity, a target-list with +5/-5 buttons per holding,
  // and a holding-form to add a holding.
  return <section data-testid="page-targets" />
}
