'use client'

export default function AddPage() {
  // TODO: render sub-form (name-input, cost-input, cycle-select, renewal-input, submit-sub)
  // with validation; on success addSubscription(...) and navigate('subscriptions').
  return <section data-testid="page-add" />
}
