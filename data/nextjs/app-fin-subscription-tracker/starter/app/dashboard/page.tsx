'use client'

export default function DashboardPage() {
  // TODO: render StatCards (monthly/annual/active/duesoon) from useSubsSummary(), plus
  // current-theme and a theme-toggle button.
  return <section data-testid="page-dashboard" />
}
