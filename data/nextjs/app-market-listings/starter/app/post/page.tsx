'use client'

export default function PostPage() {
  // TODO: render post-form with title/category/price/seller/description inputs and
  // submit-listing. Validate title (form-error if blank), addListing(...), navigate('browse').
  return <section data-testid="page-post" />
}
