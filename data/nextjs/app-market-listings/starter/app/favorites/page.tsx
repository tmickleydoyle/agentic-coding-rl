'use client'

export default function FavoritesPage() {
  // TODO: list favorited listings (favorites-list / fav-item-<id>); no-favorites when empty.
  return <section data-testid="page-favorites" />
}
