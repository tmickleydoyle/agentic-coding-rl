'use client'

export default function AddCardPage() {
  // TODO: render add-card-form with deck-select, front-input, back-input, submit-card.
  // On submit: validate both fields (form-error if blank), addCard(...), navigate('decks').
  return <section data-testid="page-add-card" />
}
