'use client'

export default function ReviewPage() {
  // TODO: if no active deck show no-deck; if no due cards show all-done; else render the
  // first due card via CardFace and grade it (gradeCard), hiding the back after grading.
  return <section data-testid="page-review" />
}
