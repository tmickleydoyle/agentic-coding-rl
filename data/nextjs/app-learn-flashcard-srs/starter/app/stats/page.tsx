'use client'

export default function StatsPage() {
  // TODO: show total-cards-value, due-today-value, and a per-deck list with
  // stat-deck-<id>-due values.
  return <section data-testid="page-stats" />
}
