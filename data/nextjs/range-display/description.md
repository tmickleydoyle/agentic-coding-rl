# Range slider display

Implement a client component `RangeDisplay` in `components/RangeDisplay.tsx`:

- A controlled `<input data-testid="slider" type="range" min="0" max="100">`.
- A `<span data-testid="value">` showing the current slider value as a string.
- Initial value: `50`.

Default export.
