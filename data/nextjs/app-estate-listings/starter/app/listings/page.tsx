'use client'

export default function ListingsPage() {
  // TODO: read filtered/stats from useListings(); render listing-count, average-price, and
  // the property-list of PropertyCards, or an empty-state when nothing matches.
  return <section data-testid="page-listings" />
}
