'use client'

export default function FavoritesPage() {
  // TODO: render favorites-count and the favorited PropertyCards (favorites-list), or a
  // favorites-empty message when there are none.
  return <section data-testid="page-favorites" />
}
