'use client'

export default function TodayPage() {
  // TODO: list tasks due today (nextDue <= today) as DueRows in a due-list (or empty-today),
  // with a due-count and a complete-<id> action per row.
  return <section data-testid="page-today" />
}
