'use client'

export default function AddPage() {
  // TODO: render add-form with title-input, schedule-select (daily/weekly) and submit-task.
  // On submit: validate the title (form-error if empty), addTask(...), navigate('all-tasks').
  return <section data-testid="page-add" />
}
