'use client'

export default function HistoryPage() {
  // TODO: list history entries as history-<id> with history-<id>-date in a history-list (or
  // empty-history), plus a history-count.
  return <section data-testid="page-history" />
}
