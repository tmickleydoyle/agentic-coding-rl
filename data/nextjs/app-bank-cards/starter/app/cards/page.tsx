'use client'

export default function CardsPage() {
  // TODO: render StatCards (limit/spent/frozen/count) and a card-list of CardRows (or
  // empty-cards when there are none).
  return <section data-testid="page-cards" />
}
