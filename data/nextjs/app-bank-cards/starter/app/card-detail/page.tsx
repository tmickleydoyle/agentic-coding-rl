'use client'

export default function CardDetailPage() {
  // TODO: if no card is selected, render no-selection. Otherwise show card-label/limit/spent/
  // remaining, a freeze-state + freeze-toggle, and a charge-list (or no-charges).
  return <section data-testid="page-card-detail" />
}
