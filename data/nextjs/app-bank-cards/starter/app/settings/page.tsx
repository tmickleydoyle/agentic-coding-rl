'use client'

export default function SettingsPage() {
  // TODO: render current-theme + theme-toggle, and per-card limit-<id> editors with a
  // limit-<id>-input and limit-<id>-save calling setLimit(id, value).
  return <section data-testid="page-settings" />
}
