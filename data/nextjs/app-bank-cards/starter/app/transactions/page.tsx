'use client'

export default function TransactionsPage() {
  // TODO: render charge-form (card-select, merchant-input, amount-input, submit-charge).
  // On submit call addCharge(...); show charge-error on failure or charge-success on ok.
  return <section data-testid="page-transactions" />
}
