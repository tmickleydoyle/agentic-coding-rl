'use client'

export default function ProjectDetailPage() {
  // TODO: when no project is selected render <p data-testid="no-project">; otherwise render
  // detail-title, detail-tags, detail-featured, and detail-feature-toggle.
  return <section data-testid="page-project-detail" />
}
