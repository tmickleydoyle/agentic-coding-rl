'use client'

export default function ProjectsPage() {
  // TODO: render tag-filter select then the filtered project-list of ProjectCards, or an
  // empty-state <p data-testid="empty-state"> when nothing matches.
  return <section data-testid="page-projects" />
}
