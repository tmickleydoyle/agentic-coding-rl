'use client'

export default function LeadsPage() {
  // TODO: status-filter select; lead-list of rows with score/status; open-<id> selects +
  // navigates to detail; empty-state when nothing matches.
  return <section data-testid="page-leads" />
}
