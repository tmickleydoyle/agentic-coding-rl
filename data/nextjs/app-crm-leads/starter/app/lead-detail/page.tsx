'use client'

export default function LeadDetailPage() {
  // TODO: no-lead when none selected; else name/source/score/status and qualify/convert/
  // lose buttons with the right enabled/disabled rules, plus the deal value if converted.
  return <section data-testid="page-lead-detail" />
}
