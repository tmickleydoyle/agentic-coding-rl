'use client'

export default function QualifyPage() {
  // TODO: avg-score; a qualify-list of new leads only with bump (+10) and qualify buttons;
  // qualify-empty when no new leads remain.
  return <section data-testid="page-qualify" />
}
