# Clear button

Implement a client component `ClearableInput` in `components/ClearableInput.tsx`:

- A controlled `<input data-testid="input">`.
- A `<button data-testid="clear">` with label `"Clear"`.
- The clear button is `disabled` when the input is empty.
- Clicking the clear button empties the input.

Default export.
