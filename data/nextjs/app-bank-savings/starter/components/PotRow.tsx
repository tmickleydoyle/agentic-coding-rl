'use client'
import type { Pot } from '../lib/types'
export default function PotRow(_props: { pot: Pot }) {
  // TODO: <li data-testid={`pot-<id>`}> name/balance/goal + progress + met badge + open button
  return <li />
}
