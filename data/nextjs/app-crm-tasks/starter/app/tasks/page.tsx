'use client'

export default function TasksPage() {
  // TODO: a new-task-form (title/contact/due/submit) with blank-title error; a task-list of
  // open tasks sorted by due date.
  return <section data-testid="page-tasks" />
}
