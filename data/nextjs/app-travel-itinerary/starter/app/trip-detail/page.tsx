'use client'

export default function TripDetailPage() {
  // TODO: render no-trip when nothing selected; otherwise detail-name/detail-total and
  // a day-<n> block per day with reorderable ActivityItems.
  return <section data-testid="page-trip-detail" />
}
