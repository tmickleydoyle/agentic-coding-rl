'use client'

export default function StepsPage() {
  // TODO: render <ul data-testid="step-list"> (step-<id>-name, step-<id>-conversion) and,
  // when a step is selected, a <div data-testid="step-detail">.
  return <section data-testid="page-steps" />
}
