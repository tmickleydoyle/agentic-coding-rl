'use client'

export default function SegmentsPage() {
  // TODO: render <ul data-testid="segment-list"> with seg-mobile and seg-desktop showing
  // each segment's overall conversion (independent of the active filter).
  return <section data-testid="page-segments" />
}
