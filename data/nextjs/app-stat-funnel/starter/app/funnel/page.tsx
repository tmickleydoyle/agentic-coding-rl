'use client'

export default function FunnelPage() {
  // TODO: render a segment-filter <select>, a funnel-rows <ul> (FunnelRow per step), and an
  // overall-conversion <span>.
  return <section data-testid="page-funnel" />
}
