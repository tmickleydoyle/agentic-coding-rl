'use client'

export default function SettingsPage() {
  // TODO: render a toggle-theme button, a current-theme span, and a default-segment <select>
  // bound to the shared segment.
  return <section data-testid="page-settings" />
}
