'use client'

export default function RoomPage() {
  // TODO: render the selected room (room-title, message-list, send-form). Show
  // no-room-selected when nothing is selected.
  return <section data-testid="page-room" />
}
