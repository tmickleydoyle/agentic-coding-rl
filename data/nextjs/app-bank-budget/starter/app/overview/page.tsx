'use client'

export default function OverviewPage() {
  // TODO: read totals from useBudgetSummary(); render StatCards (limit/spent/remaining/
  // overlimit), the currency-label, and an overall-alert or overall-ok message.
  return <section data-testid="page-overview" />
}
