'use client'

export default function BudgetsPage() {
  // TODO: per category render a budget-<id> block with a limit-input and a save button that
  // calls setLimit(id, value).
  return <section data-testid="page-budgets" />
}
