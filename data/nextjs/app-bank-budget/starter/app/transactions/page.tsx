'use client'

export default function TransactionsPage() {
  // TODO: render txn-form (category-select, description-input, amount-input, submit-txn) with
  // validation (form-error), then a txn-list with per-row remove buttons (or empty-txns).
  return <section data-testid="page-transactions" />
}
