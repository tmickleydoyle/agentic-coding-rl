'use client'

export default function AddPage() {
  // TODO: render snippet-form (title-input, language-input, code-input, save-snippet). On
  // submit: validate title+language (form-error), addSnippet, navigate('snippets').
  return <section data-testid="page-add" />
}
