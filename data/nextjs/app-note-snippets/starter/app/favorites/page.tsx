'use client'

export default function FavoritesPage() {
  // TODO: render favorites-list of favorite-<id> rows (title + unfav-<id>) or
  // favorites-empty when there are none.
  return <section data-testid="page-favorites" />
}
