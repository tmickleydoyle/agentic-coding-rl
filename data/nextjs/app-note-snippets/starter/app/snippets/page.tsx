'use client'

export default function SnippetsPage() {
  // TODO: render search-input, language-filter select, and the snippet-list of snippet-<id>
  // rows (title, language, open-<id>, fav-<id>) or snippets-empty when nothing matches.
  return <section data-testid="page-snippets" />
}
