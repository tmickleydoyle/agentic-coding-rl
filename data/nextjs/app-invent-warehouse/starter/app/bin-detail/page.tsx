'use client'

export default function BinDetailPage() {
  // TODO: show the selected bin detail + item-list (or bin-empty) + go-move, or a
  // no-selection message.
  return <section data-testid="page-bin-detail" />
}
