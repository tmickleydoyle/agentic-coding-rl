'use client'

export default function MapPage() {
  // TODO: show current-theme, a theme-toggle, an overall-usage line, a bin-count line, and
  // the map-grid of cells.
  return <section data-testid="page-map" />
}
