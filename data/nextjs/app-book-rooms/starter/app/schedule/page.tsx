'use client'

export default function SchedulePage() {
  // TODO: render a schedule group per room with a count and the room's bookings.
  return (
    <section data-testid="page-schedule">
      <h1>Schedule</h1>
    </section>
  )
}
