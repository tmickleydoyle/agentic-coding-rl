'use client'

export default function SavedPage() {
  // TODO: render saved-count and a saved-list of PropertyCards (with monthly), or
  // a saved-empty message when there are none.
  return <section data-testid="page-saved" />
}
