'use client'

export default function ThreadPage() {
  // TODO: render the selected thread (thread-text, thread-status, resolve-toggle,
  // reply-list, reply-form). Show no-thread-selected when nothing is selected.
  return <section data-testid="page-thread" />
}
