'use client'

export default function NewQuotePage() {
  // TODO: render new-quote-form with client-input, desc-input, qty-input, price-input,
  // submit-quote. On submit: validate client (form-error if empty), addQuote(...), select
  // it, navigate('quote-detail').
  return <section data-testid="page-new" />
}
