'use client'

export default function QuotesPage() {
  // TODO: render status-filter select then the filtered quote-list of QuoteRows, or an
  // empty-state <p data-testid="empty-state"> when nothing matches.
  return <section data-testid="page-quotes" />
}
