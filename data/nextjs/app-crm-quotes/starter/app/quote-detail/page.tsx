'use client'

export default function QuoteDetailPage() {
  // TODO: when no quote is selected render <p data-testid="no-quote">; otherwise render
  // detail-client, detail-status, detail-total, an item-list with item-<i>-subtotal, and
  // set-<status> buttons.
  return <section data-testid="page-quote-detail" />
}
