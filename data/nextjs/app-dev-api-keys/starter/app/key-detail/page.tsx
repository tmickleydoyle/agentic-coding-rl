'use client'

export default function KeyDetailPage() {
  // TODO: if no key selected render <p data-testid="no-selection">; otherwise show
  // detail-name, detail-secret (masked), detail-scopes (scope-<name>), detail-usage,
  // detail-status, a revoke-btn (only while active), and a use-btn.
  return <section data-testid="page-key-detail" />
}
