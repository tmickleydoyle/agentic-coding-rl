'use client'

export default function CreateKeyPage() {
  // TODO: render create-key-form with name-input, scope-read/scope-write/scope-admin
  // checkboxes, and submit-key. On submit: validate name (form-error if empty),
  // createKey(...), navigate('keys').
  return <section data-testid="page-create-key" />
}
