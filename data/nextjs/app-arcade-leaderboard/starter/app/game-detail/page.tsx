'use client'

export default function GameDetailPage() {
  // TODO: render the selected game's name (detail-name) and its ranked score-list.
  // Show no-game-selected when nothing is selected, and no-scores when empty.
  return <section data-testid="page-game-detail" />
}
