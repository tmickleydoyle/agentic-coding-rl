'use client'

export default function SubmitPage() {
  // TODO: render a submit-form (game-select, player-input, points-input, submit-score).
  // On submit call submitScore and openGame, or show submit-error.
  return <section data-testid="page-submit" />
}
