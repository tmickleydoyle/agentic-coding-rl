'use client'

export default function OverviewPage() {
  // TODO: render a region-filter <select> and StatCards (revenue/units/orders/top-product)
  // computed from the region-filtered orders.
  return <section data-testid="page-overview" />
}
