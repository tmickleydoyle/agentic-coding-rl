'use client'

export default function RegionsPage() {
  // TODO: render <ul data-testid="region-list"> from byRegion (always all three regions)
  // with region-<region>-revenue and region-<region>-units.
  return <section data-testid="page-regions" />
}
