'use client'

export default function ProductsPage() {
  // TODO: render <ul data-testid="product-list"> (ProductRow per product, revenue desc)
  // and, when a product is selected, a <div data-testid="product-detail">.
  return <section data-testid="page-products" />
}
