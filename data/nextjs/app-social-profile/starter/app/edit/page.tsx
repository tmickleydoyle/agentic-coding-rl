'use client'

export default function EditPage() {
  // TODO: render edit-form with name-input (prefilled), bio-input (prefilled), save-profile.
  // On submit: validate name (edit-error if empty), updateProfile(meId, ...), navigate('profile').
  return <section data-testid="page-edit" />
}
