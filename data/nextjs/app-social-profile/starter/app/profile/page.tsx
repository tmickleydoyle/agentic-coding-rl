'use client'

export default function ProfilePage() {
  // TODO: render profile-name/bio, profile-stats (StatBlocks), and a follow-toggle
  // when viewing someone other than me.
  return <section data-testid="page-profile" />
}
