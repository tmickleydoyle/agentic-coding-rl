'use client'

export default function CreatePage() {
  // TODO: render a create-form (title-input, day-input, category-select, submit-create);
  // form-error on a blank title or out-of-range day, otherwise addEvent and navigate to month.
  return (
    <section data-testid="page-create">
      <h1>Create Event</h1>
    </section>
  )
}
