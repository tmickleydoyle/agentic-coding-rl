'use client'

export default function MonthPage() {
  // TODO: render a category-filter select and a month-grid (cell-blank-<i> for leading nulls,
  // day-<n> buttons via DayCell with day-<n>-count of visible events).
  return (
    <section data-testid="page-month">
      <h1>Month</h1>
    </section>
  )
}
