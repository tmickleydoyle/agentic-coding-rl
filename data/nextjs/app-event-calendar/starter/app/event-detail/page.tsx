'use client'

export default function EventDetailPage() {
  // TODO: show day-heading and the visible events on the selected day (event-<id> rows via
  // EventRow); no-events when none, no-day when nothing selected.
  return (
    <section data-testid="page-event-detail">
      <h1>Day</h1>
    </section>
  )
}
