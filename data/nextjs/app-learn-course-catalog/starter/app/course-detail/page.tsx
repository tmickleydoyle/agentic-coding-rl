'use client'

export default function CourseDetailPage() {
  // TODO: if no selected course show no-course; else detail-title + enroll-toggle, and a
  // lesson-list (LessonRows) only when enrolled.
  return <section data-testid="page-course-detail" />
}
