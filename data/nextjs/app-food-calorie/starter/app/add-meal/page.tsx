'use client'

export default function AddMealPage() {
  // TODO: add-meal-form with name/calories/macros; validate then addMeal + navigate('today').
  return <section data-testid="page-add-meal" />
}
