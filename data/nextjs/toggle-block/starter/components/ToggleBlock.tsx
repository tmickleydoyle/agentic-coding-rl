'use client'
import { useState } from 'react'

export default function ToggleBlock({ text }: { text: string }) {
  // TODO: track visibility, conditionally render the block, swap button label.
  return (
    <div>
      <button>Show</button>
    </div>
  )
}
