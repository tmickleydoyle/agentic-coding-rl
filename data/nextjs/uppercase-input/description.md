# Uppercase echo

Implement a client component `UppercaseInput` in `components/UppercaseInput.tsx`:

- Controlled `<input data-testid="input">`.
- An element `data-testid="echo"` showing the input value converted to UPPERCASE.
- When the input is empty, the echo element contains an empty string.

Default export.
