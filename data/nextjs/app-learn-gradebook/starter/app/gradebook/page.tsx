'use client'

export default function GradebookPage() {
  // TODO: render a grade-<sid>-<aid> input per student×assignment (setGrade/clearGrade) and
  // an avg-<sid>-value per student.
  return <section data-testid="page-gradebook" />
}
