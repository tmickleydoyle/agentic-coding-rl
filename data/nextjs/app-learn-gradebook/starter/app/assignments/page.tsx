'use client'

export default function AssignmentsPage() {
  // TODO: render an assignment-list and an add-assignment-form (assignment-title-input,
  // add-assignment) that validates blank titles (assignment-error) and adds.
  return <section data-testid="page-assignments" />
}
