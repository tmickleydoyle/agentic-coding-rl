'use client'

export default function StudentsPage() {
  // TODO: render a student-list of StudentRows and an add-student-form (student-name-input,
  // add-student) that validates blank names (student-error) and adds via addStudent.
  return <section data-testid="page-students" />
}
