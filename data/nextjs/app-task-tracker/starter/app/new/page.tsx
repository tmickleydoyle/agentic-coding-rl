'use client'

export default function NewTaskPage() {
  // TODO: render new-task-form with title-input, project-select, due-input, submit-task.
  // On submit: validate title (form-error if empty), addTask(...), navigate('tasks').
  return <section data-testid="page-new" />
}
