'use client'

export default function DashboardPage() {
  // TODO: read counts from useTasks(); render StatCards (total/completed/active) and
  // per-project counts (project-count-<id>-value).
  return <section data-testid="page-dashboard" />
}
