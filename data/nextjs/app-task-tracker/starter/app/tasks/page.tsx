'use client'

export default function TasksPage() {
  // TODO: render <Filters/> then the filtered task list (task-list of TaskItems) or an
  // empty-state <p data-testid="empty-state"> when nothing matches.
  return <section data-testid="page-tasks" />
}
