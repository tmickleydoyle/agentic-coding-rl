'use client'

export default function TriagePage() {
  // TODO: render label-filter, priority-filter, and assignee-filter selects then the
  // filtered triage-list (triage-<id>) or an empty-state when nothing matches.
  return <section data-testid="page-triage" />
}
