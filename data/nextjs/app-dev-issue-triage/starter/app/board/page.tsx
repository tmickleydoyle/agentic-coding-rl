'use client'

export default function BoardPage() {
  // TODO: render col-open / col-in-progress / col-closed columns with col-<status>-count
  // and one card-<id> per issue, each with a next-<id> button advancing the status.
  return <section data-testid="page-board" />
}
