'use client'

export default function IssueDetailPage() {
  // TODO: if no issue selected render <p data-testid="no-selection">; otherwise show
  // detail-title, detail-labels (label-<name>), detail-priority, detail-assignee, an
  // assignee-input + assign-btn, and a priority-select.
  return <section data-testid="page-issue-detail" />
}
